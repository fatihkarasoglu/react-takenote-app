import { NavLink } from "react-router-dom"

export default function Login() {

    return(
        <div>
            Login Page test
            <NavLink to="/">Ana sayfaya dön - </NavLink>
        </div>
    )
}